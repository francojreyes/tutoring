#include <stdio.h>

#include "list.h"
#include "list.h"

int main(void) {
    // hello
    List l = NULL;
    l = listAppend(l, 1);
    l = listAppend(l, 2);
    l = listAppend(l, 3);
    l = listAppend(l, 4);
    l = listAppend(l, 5);
}