#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>

bool isPalindrome(char *s);

int main(int argc, char **argv) {
    printf("%s\n", isPalindrome(argv[1]) ? "true" : "false");
    return 0;
}

bool isPalindrome(char *s) {
    // int n = strlen(s);
    return false;
}
